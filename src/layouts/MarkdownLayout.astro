---
import Layout from '~/layouts/PageLayout.astro';

import type { MetaData } from '~/types';
import { createBreadcrumbSchema } from '~/utils/structured-data';

export interface Props {
  frontmatter: {
    title?: string;
    description?: string;
    robots?: MetaData['robots'];
  };
}

const { frontmatter } = Astro.props;

const metadata: MetaData = {
  title: frontmatter?.title,
  description: frontmatter?.description,
  robots: frontmatter?.robots,
  structuredData: createBreadcrumbSchema([{ name: 'Home', item: '/' }, { name: frontmatter?.title || 'Page' }]),
};
---

<Layout metadata={metadata}>
  <article class="mx-auto max-w-4xl px-4 py-12 sm:px-6 sm:py-16 lg:py-20">
    <div
      class="prose prose-md max-w-none dark:prose-invert dark:prose-headings:text-heading sm:prose-lg prose-h1:font-heading prose-h1:text-4xl prose-h1:leading-tight prose-a:text-link prose-li:my-0"
    >
      <h1>{frontmatter.title}</h1>
      <slot />
    </div>
  </article>
</Layout>
